# Python dependencies for the Image Classification API
fastapi==0.95.2
uvicorn==0.23.2
# tensorflow is already installed in the base image
pillow==10.0.0
python-multipart==0.0.6
# numpy is already installed in the tensorflow image
typing-extensions==4.5.0
pydantic==1.10.8
jinja2==3.1.2

